const INFLATION_MULTIPLIER = 1.1267; // inflates 2010 dollars to 2017 dollars
const PREVIOUS_COLS = ['sum', 'm', 'cv', 'codingThreshold'];
const STANDARD_ERROR = (designFactor) => designFactor * ((
    (93 / (7 * sum)) * 2500
  ) ** 0.5);



module.exports = {
  INFLATION_MULTIPLIER,
  PREVIOUS_COLS,
};
